[gd_scene load_steps=4 format=2]

[ext_resource path="res://Graphics/Oil2.png" type="Texture" id=1]

[sub_resource type="GDScript" id=1]
script/source = "extends Node2D

# Declare member variables here. Examples:
# var a = 2
# var b = \"text\"

# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.

# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass

var timer    = 0
var finisher = 9

var wait = 6

var l = []

func _process(delta):
	$Oil.scale += Vector2(1.5,1.5) * delta 
	if $Oil.scale.x > 1.5: 
		timer += delta
		$Oil.scale    = Vector2(1.5,1.5)
		if timer < wait : return
		var alpha_scale = max( 1.0 - (timer/finisher), 0 ) 
		$Oil.modulate = Color( 1,1,1, alpha_scale )
		if alpha_scale == 0: 
			call_deferred(\"queue_free\")
			l = []
	for body in l:
		body.speed = 10

func _on_Area2D_body_entered(body):
#	if body.is_in_group(\"player\") or body.is_in_group(\"enemy\"):
#		l.append(body)
	
	
	pass # Replace with function body.
"

[sub_resource type="CircleShape2D" id=2]
radius = 139.064

[node name="Node2D" type="Node2D"]
script = SubResource( 1 )

[node name="Oil" type="Sprite" parent="."]
position = Vector2( 0.296755, -0.251169 )
scale = Vector2( 0.01, 0.01 )
z_index = 100
texture = ExtResource( 1 )

[node name="Area2D" type="Area2D" parent="Oil"]
collision_layer = 8
collision_mask = 8

[node name="CollisionShape2D" type="CollisionShape2D" parent="Oil/Area2D"]
shape = SubResource( 2 )
[connection signal="body_entered" from="Oil/Area2D" to="." method="_on_Area2D_body_entered"]
